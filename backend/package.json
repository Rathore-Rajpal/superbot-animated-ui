{
  "name": "superbot-backend",
  "version": "1.0.0",
  "description": "Backend for Superbot Animated UI with MySQL database integration",
  "main": "src/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon src/server.js",
    "build": "npm install --production && mkdir -p dist && cp -r src/ dist/ && cp package*.json dist/ && cp .env* dist/ || true",
    "prestart": "npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
